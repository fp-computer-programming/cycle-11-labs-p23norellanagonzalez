# Author NDO 02/02/23

#Create a variable named weekdays and set it equal to a tuple that contains the days Monday-Friday as strings
weekdays = ('Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday')

#Create a variable named weekends and set it equal to a tuple that contains the days Saturday and Sunday as strings
weekends = ('Saturday', 'Sunday')

#Create a variable named days_of_the_week and set it equal to  that the concatenation of the two previous variables
days_of_the_week = (weekdays + weekends)

#Print the variable days_of_the_week and should display a tuple containing the days of the week in order 
print(days_of_the_week)